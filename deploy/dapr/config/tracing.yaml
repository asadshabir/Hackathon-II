apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: tracing-config
  namespace: todo-app
spec:
  tracing:
    samplingRate: "1"
    otel:
      endpointAddress: "jaeger-collector.todo-app.svc.cluster.local:4317"
      isSecure: false
      protocol: grpc
  metric:
    enabled: true